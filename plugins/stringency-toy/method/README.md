# toy method repo

The method repository for the `stringency-toy` domain. Tests copy this directory into a
temporary git repository, commit it, and `init` a project against it. It also serves as the
method-repo template: `pipelines/`, `modules/`, `policy.yml`, `envs/`, `notes/`, `controls/fixtures/`.

# Track 1h: project page and notify on hold

Session 2026-09-23 14:20 onward on PROTSEQ, branch `track-1h` (worktree
`~/mytools/stringency/stringency-track1h` from `origin/main` at `d3747c2`, PR #10 merged). Brief:
`~/mytools/stringency/brief-track1h.md`. Plan: `spec/plans/project-page-and-notify.md`. This
session owns `src/stringency/`; the Lane E worktree is read, not edited.

## Goal

Build Track 1h in the plan's order: notify on the four events (plan section 4), the project page
on `review --serve` (section 3), tag and reinstall beside `0.2.4-sc0.1.8`, the skill and host
text (3.5), the measurement checklist (6.4), the roadmap lines. Owner decisions taken before the
session: email only (campus relay first, Office 365 fallback), the `teams_webhook` kind built but
not configured, no Slack; every number from an engine output through one function; verdicts only
under the account whose process serves the form; notify on the four events only, never changing
a verb's exit code.

## Done

- Worktree made, `uv sync --all-groups`; plan, UX note 2, 5 and 6, roadmap 1h, design 7.5, 14.1,
  14.2, 14.4, the Lane E note, and the code to extend read.
- Preflight facts: `smtp.service.emory.edu:25` and `smtp.office365.com:587` both answer a 220
  banner from PROTSEQ (TCP connect only; nothing sent). Ports 8000 and 8001 are this machine's
  Claude Science, 8080 is a python process; 8765 is free. `systemd --user` runs for `jrrose5` but
  `Linger=no`, so a user unit stops at logout until `loginctl enable-linger` (needs sudo, once).
  `~/.config/stringency` does not exist yet. One `STATUS.md` exists, under `/lab/projects/Lyons_CLP`.

- Owner's answers (2026-09-23, in chat): (1) keep everything on `review --serve`, no new verb;
  (2) a standing read-only instance, loopback port 8766 plus tunnel, as a `systemd --user` unit;
  (3) the running user only is notified in v1; (4) one test send to the owner's address allowed.
- Notify: `src/stringency/notify.py` (config, four events, the three kinds, the log, the hook),
  `notify_after` beside `refresh_if_present` in `init`, `run`, `submit` (both branches),
  `review` (single and batch), `deliver`, `abandon`; `tests/conftest.py` points
  `XDG_CONFIG_HOME` at a temp dir for every test; `tests/test_notify.py` (ten tests, plan 4.4 and
  the two network kinds with fakes). One test send through `smtp.service.emory.edu:25` from the
  engine's `send_email`: accepted (the owner's address, 2026-09-23 ~14:55).
- Two DECISIONS lines (config location and kinds; the log as the de-duplication record).
- PR #11 (notify) opened against `main`.
- The project page: `present.present_rows` and `present.render_html` (the sections over the same
  rows the markdown renderer prints), `board.row` and `board.COLUMNS` (the cells `STATUS.md` and
  the page share), `review_serve.py` rewritten in place with the landing page ("Needs you", then
  the board), `/p/<i>`, `/p/<i>/run/<run_id>`, `/p/<i>/file/<run_id>/<name>` (names from
  `index.json` only, separators refused), `/hold/<id>` and `/run/<id>` redirects, the `hold_view`
  tables on the hold page, `--read-only` (405 naming the two verdict routes, no form) and
  `--token-file` (created mode 600 when missing) on `review`; six new tests in
  `tests/test_review_serve.py`. `scripts/review-page.sh` passes extra flags through;
  `scripts/stringency-page.service` is the standing read-only unit (loopback 8766, not started;
  the owner installs it). Smoke-tested read-only against the delivered toy project on a loopback
  port: board row, the comparison table from the delivery skill, the TSV served as
  `text/tab-separated-values`, POST 405, `/run/<id>` 302. Two DECISIONS lines.

## Learned

- `submit` and `next_step` do not fill `plain` (only `run_loop` does); notify computes it from the
  trace with `plain_next` when the verb did not.
- The confirm hold's title is "the plan" in the message; there is no step to name.

## Altered

- The hold page does not repeat the confirm echo-back through `present --hold`: the packet
  `review_render.render` prints for a confirm hold already is the echo-back. Flag and item holds
  get the method's `hold_view` tables above the packet as the plan says.
- The `systemd` unit lists `--projects /data/lab/projects` only (depth two); a deeper project
  such as the toy at `.../lane-e/group-a-vs-b` needs a `--project` line, which the unit's header
  says. A missing `--project` path would stop the unit, so none is hard-coded.

## Open

1. Reinstall (brief step 4), skill text (5), measurement checklist (6), roadmap (7).

## Verify

    cd ~/mytools/stringency/stringency-track1h
    uv run pytest && uv run ruff check . && uv run ruff format --check . && uv run mypy && scripts/check_no_biology.sh
